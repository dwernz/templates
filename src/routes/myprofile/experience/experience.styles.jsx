import styled from 'styled-components';

export const ContentItem = styled.div`
    margin-bottom: 40px;

    h3 {
        margin: 0 0 10px;
        line-height: 1;
        font-weight: bold;
        text-transform: uppercase;
    }

    h4 {
        margin: 0;
        line-height: 1;
    }

    small {
        color: #888;
    }

    p {
        margin: 0;
    }
`;