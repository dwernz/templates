import styled from "styled-components";
import Toggle from "../../components/inputs/toggle/toggle.component";

export const BinaryContainer = styled.div`
    display: flex;
`;

export const BinaryToggle = styled(Toggle)`
    margin: 5px;
`